apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "summary-app-charts.fullname" . }}-test-connection"
  labels:
    {{- include "summary-app-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "summary-app-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
